import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RecursosPage } from './recursos';

@NgModule({
  declarations: [
    RecursosPage,
  ],
  imports: [
    IonicPageModule.forChild(RecursosPage),
  ],
})
export class RecursosPageModule {}
